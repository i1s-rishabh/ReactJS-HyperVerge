const domContainer = document.querySelector("#container");
reactDOM.render(<h1>Hello, World!</h1>, domContainer) 

// FIRST parameter of .render() method
    // what is want to render in the first parameter i've to write.

// SECOND parameter of .render() method
    // where do i want ot render it.