import React from 'react';

function Paragraph() { // this file contains a paragraph component
    return <p>We can create multiple react Components as we want but we need to remember always component file name's first letter must be capital</p>;
}

export default Paragraph;